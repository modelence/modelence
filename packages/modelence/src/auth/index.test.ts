// TODO: Add tests for auth module exports
// Should test: user stores, signup/login/password reset mutations
describe('auth/index', () => {
  test('placeholder', () => {
    expect(true).toBe(true);
  });
});
