{
  "spec": {
    "build": {
      "install": "npm install",
      "command": "npm run build"
    }
  },
  "notes": [
    "No package-lock.json found; dependencies are installed with `npm install`.",
    "The `start` script runs `ng serve`, a development server, which is not deployed; this looks like an Angular app, whose build output directory is set in angular.json; set web.static in modelence.json to it (usually dist/<project>/browser).",
    "No `start` script or Procfile found; set web.start in modelence.json or pass --start-command."
  ],
  "profile": null
}
