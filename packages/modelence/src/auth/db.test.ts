// TODO: Add tests for MongoDB collections
// Should test: usersCollection, emailVerificationTokens, resetPasswordTokens, disposableEmails
describe('auth/db', () => {
  test('placeholder', () => {
    expect(true).toBe(true);
  });
});
