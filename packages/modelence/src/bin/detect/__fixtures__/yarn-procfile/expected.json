{
  "profile": null,
  "spec": {
    "build": {
      "install": "yarn install --frozen-lockfile",
      "command": "yarn build"
    },
    "web": {
      "start": "node server.js --port $PORT"
    }
  },
  "notes": []
}
