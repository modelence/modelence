{
  "profile": "lovable",
  "spec": {
    "build": {
      "install": "npm install",
      "command": "npm run build"
    },
    "web": {
      "static": [
        {
          "path": "/",
          "dir": "dist"
        }
      ]
    }
  },
  "notes": [
    "package-lock.json is out of date with package.json, so dependencies are installed with `npm install`. Run `npm install` locally and commit the lockfile to get reproducible `npm ci` installs.",
    "No start script found; the site is served from dist/ with single-page app fallback.",
    "Lovable project using Supabase: set VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY in the environment variables (they are inlined at build time)."
  ]
}
