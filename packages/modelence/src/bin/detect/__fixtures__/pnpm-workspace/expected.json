{
  "profile": null,
  "spec": {
    "build": {
      "install": "npm install -g pnpm@10.4.1 && pnpm install --frozen-lockfile",
      "command": "pnpm run build"
    },
    "web": {
      "start": "pnpm --filter api start"
    }
  },
  "notes": [
    "No root start script; the container starts the api workspace package (apps/api/), and the root build script is preserved."
  ]
}
