{
  "spec": {
    "build": {
      "install": "npm install",
      "command": "npm run build"
    },
    "web": {
      "static": [
        {
          "path": "/",
          "dir": "build"
        }
      ]
    }
  },
  "notes": [
    "No package-lock.json found; dependencies are installed with `npm install`.",
    "The `start` script runs `react-scripts start`, a development server, which is not deployed; the site is served from build/ with single-page app fallback instead. Set web.start in modelence.json to deploy a server process instead."
  ],
  "profile": null
}
