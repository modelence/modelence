// TODO: Add tests for Module class
// Should test: queries, mutations, stores, cron jobs, rate limits registration
describe('app/module', () => {
  test('placeholder', () => {
    expect(true).toBe(true);
  });
});
